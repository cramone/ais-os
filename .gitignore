.env
.env.local
.mcp.json
gitignored/
.DS_Store
*.log
__pycache__/
node_modules/
.claude/settings.local.json
audits/
.obsidian/
nul
*.pyc
.pytest_cache/

# Large data exports — keep on disk, never commit (GitHub 100MB limit)
projects/magiq-media/files/export/MAGIQ_DOCS_export.json
projects/magiq-media/files/export/*-data.7z

# Tower runtime state (interrupts, todos, levelup) — written by the app at
# runtime; tracking it dirties the tree and blocks deploy-cortex.sh's pull.
tower/data/
