# document-lifecycle-cleaner

## Project Overview

Yearly automated process to cull documents and folders from MAGIQ Documents based on a calendar year-end cutoff date. Targets a specific facility identified by folder naming conventions (acronyms). Client: NATA.

**Current status:** As-built. Epic 34120 is implemented and merged — the archival/cleanup pipeline, the React SPA, dual IIS/Docker hosting, and the operator/admin surfaces. The spec (`spec/NATA_Document_Lifecycle_Cleaner_Spec_v0.6.md`, aligned to the repo 2026-08-07) describes the system as built. The **Phase 3 — Name Normalization (Step 8)** phase is now implemented in the working tree (`decisions/log.md` [2026-08-05]): SOAP Get/Update Folder+Document ops, `RunPhase.Normalization`, `dbo.CleanupRunRename` + `EnteredNormalization` audit-lock (consolidated into `0001_baseline.sql` — the interim `0002–0004` scripts were folded into the single baseline), the `RenamePlanner` + `ExecuteNormalizationAsync` phase, and confirm→normalization→archival chaining. **Step numbering:** renumbered so nothing collides with Normalization — Normalization = Step 8, Archival = Steps 9–10, Cleanup = Steps 11–12; `RunPhaseExecutor` emits exactly these numbers (`decisions/log.md` [2026-08-05]). Renames address items by their current (raw) path and repath pending descendants after each folder rename. **2026-08-10:** the Step 8 gate was widened from conflicts-only to **any name change** — the run pauses at the Normalization Review whenever the plan changes a name, the operator reviews the **full folder/document change list** (now on `GET …/normalization/plan`) and can **download it before → after (CSV/Excel)** via `GET …/normalization/changes/export`, then **confirms** (`POST …/normalization/confirm`) before 8b runs; only a zero-change plan auto-continues, and the audit lock still trips on the first executed 8b change (`decisions/log.md` [2026-08-10]; plan `normalization-change-review-plan.md`). **2026-08-10:** Step 10 gained an operator **move-failure retry** — a Document move failures panel lists unmovable documents and the operator retries them individually/selected/all (`GET`/`POST …/archival/move-failures[/retry]`); clearing the last failure **pauses after archival** (`AwaitingInput`) so the operator proceeds to cleanup explicitly via `POST …/archival/continue` rather than auto-chaining the irreversible purge (`decisions/log.md` [2026-08-10]; plan `move-failure-retry-plan.md`). **2026-08-14 → 2026-08-17:** **Step 12 completed** (spec rev 23) — the purge now also destroys the **source folders Step 11 deleted** (soft-deleted, so they were all still in the recycle bin; matched on leaf name *and* parent `DeletePath`, failing closed on an unrecognised bin shape), emits **per-item progress + per-item and summary audit rows** (it previously showed 0/1 for a seven-minute job), and offers a **deleted folder manifest** — `GET /runs/{runId}/folders/deleted/export`, a CSV of every folder removed from the source library, ordered shallowest-first for hand re-creation, prompted for in the typed purge confirmation because afterwards it is the only record left. It restores paths and the two delete rules only; description/owner/security are never read by the tool and the file says so. Both the purge list and the manifest derive from one definition — `DeletedSourceFolders.From` over the **audit trail** (Cleanup-phase `DeleteFolder`/`Ok` rows), not `CleanupRunFolder` rows, because the empty-subtree prune's folders have no row and are exactly the empty folders both were missing (`decisions/log.md` [2026-08-14, 2026-08-17]). **2026-08-17 (spec rev 24), from the first live purge under the above:** `DeleteDomain` leaves the archive library as **one very large recycle-bin entry** (not the scattered per-item entries the 34525 training note recorded — spec corrected; it also means the archive half's progress can only read 0 → 1). Purging it outlived the 100s SOAP timeout, was **retried four times** — each retry starting another destroy — and was reported as failed although MAGIQ had completed it. Now: a `SoapOperationPolicy` per operation (`HttpClient.Timeout` = `InfiniteTimeSpan`, deadline on a per-request linked CTS) giving `DeleteDomain`/`PurgeRecycleBinItem` their own `MagiqPurgeTimeoutSeconds` (default 30 min) and **no transport retry**; a transport failure there is `MagiqSoapErrorKind.Indeterminate` — *sent, no answer, outcome unknown* — which the teardown resolves by **re-reading the recycle bin**, failing closed if it can't (`decisions/log.md` [2026-08-17]). Active work is the deferred/post-MVP backlog (`deferred-work-plan.md`) and the **operation audit trail + before/during/after Document Register (CSV)** polish (all `decisions/log.md` [2026-08-05]). **2026-08-17 (spec rev 25):** an operator-surface pass — **first-run setup now captures the MAGIQ Documents connection string** (required, own `POST /setup/magiq/test-database`, opened server-side and written *before* the endpoint/allowlist pair that marks setup configured, so setup can't complete without it; it was previously deferred to System Settings, leaving an install that passed sign-in and failed at the first query) — plus SPA changes: animation now marks **only a `Running` run** (badge, stepper step, transport chip, audit follow indicator; one stylesheet, off under `prefers-reduced-motion`), "Polling…" became **"Tailing"**, live-activity rows carry the **full path + error on their own lines**, the trail's **Destination path** composes a rename's new name with the source's parent at render time, the Normalization Review **summarises the change list** behind *Show all*, and pending conflicts are **grouped by the decision they pose** (identical / different content / folders) (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 26):** the same treatment for the Step 8b **execution** panel — summary (progress, counts, per-kind folders/documents) with **Show detail** over the per-item table (collapsed by default, auto-opens once if the phase stops with failures), and **Confirm & continue to archival** moved to a footer **after** the content with a status line, matching every other operator gate (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 27):** two audit-trail fixes — the shared `BuildFilter`'s `Phase NOT IN (…)` dropped every **null-phase** row (SQL: `NULL NOT IN` is NULL, not true), so **all rollback rows and all Step 12 purge/teardown rows** were invisible in the feed, trail, count and CSV export; and the surfaces now **tail in any non-terminal state** (`Running`/`AwaitingInput`/`Failed` — rollback, retries, prune and purge all run at a stopped run), with the tick refreshing **rows** rather than only the count. Supersedes the [2026-08-13] "poll only while Running" and [2026-08-14] count-only decisions (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 28):** liveness is **no longer inferred from run status at all** — a rollback runs against a `Cancelled` run (terminal by status, busy in fact), which broke the status-list approach for the third time. `useTailClock` **always polls and adapts speed** (5s while the trail grows, 15s when it doesn't), detecting activity from the trail itself; status is only a hint, so being wrong costs one tick, not a frozen view. `useRunProgress` stamps `lastMessageAt` and the shared `isWorking()` drives the Run progress live chip. The status badge deliberately stays factual (`Cancelled`, unanimated) (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 29):** steps are now **named for what they do** (`theme/steps.ts` — Step 11 = "Empty folder cleanup", Step 12 = "Purging deleted items"; "Cleanup (step 11)" hid the soft-delete/irreversible-purge boundary), and the progress contract carries an **`operation`** naming the individual *pass* (`RunOperations.cs`; `PhaseStarted`/`ProgressUpdated`, optional + additive) — because **a step is not one pass**: Step 11 runs "Deleting empty folders" then "Pruning empty subfolders", Step 12 the archive then the source folders, a rollback three, each restarting the count, so the bar fills more than once per step (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 30):** Step 11's pre-delete guard refusal ("still has N subfolder(s)") is audited as a new **`Deferred`** outcome, not `Failed` — it's the designed first outcome for a folder whose subtree Step 4 never evaluated, and the prune pass clears it, so a healthy run was filling the feed with red rows it then resolved itself; reason moves to `Detail`, no `ItemFailed`, and the **folder row still goes to `Failed`** (the durable handoff to the prune + what keeps an unresolved deferral visible). A guard refusal also no longer drags the parent through relax→retry→restore — a rule change can't make a live subfolder disappear (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 31):** the library/folder pickers show **placeholder rows** while listing instead of collapsing to "Loading…" and re-expanding (shared `PickerSkeleton`; geometry from the caller's own `rowStyle`, row count remembered from the last listing, clamped 3–6) (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 32):** the same pickers also **hold their list height while filtering** — `useReservedListHeight` measures the *unfiltered* list (clamped to its own scroll cap) and the "no matches" message renders inside the reserved box; `PickerSkeleton.tsx` → `components/PickerList.tsx` (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 33):** the Start-a-run form puts the **year-end cutoff first** (with a description of what the date does), and the source picker carries a **required label** (*Library or folder to cull*, asterisked like the cutoff) plus a description **under the Library/Folder tabs** of what each selects (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 34):** the shared folder browser puts the **filter box above the breadcrumb** and folds *Change library* into the breadcrumb row as an **up-folder icon button** — up one level, or out to the library list at the root (`onExitLibrary`) (`decisions/log.md` [2026-08-17]). **2026-08-17 (spec rev 35):** the **phase log moved into the phase stepper's card** behind a closed-by-default *Phase timeline* disclosure and its own bottom card was deleted — same timeline at a different resolution, and as a card it occupied every active run with a table nobody works with (`decisions/log.md` [2026-08-17]). **2026-08-18 (spec rev 36):** **Re-run from Step 1** now clears the previous attempt's derived state before rewinding (`ICleanupRunStore.ClearRerunStateAsync`, one transaction: `RegisterExport`, `CleanupRunRename`, `CleanupRunNameConflict`+`Item`) — the run keeps its id, so the abandoned attempt's pinned Document Register snapshots showed as the new attempt's, and (found while fixing that) Step 8a only plans when no rename rows exist, so a re-run **re-applied the previous rename plan** instead of re-planning against the current library. The **audit trail and phase log are kept** — only re-derivable state is cleared; the clear runs *before* the rewind so a failure leaves the run terminal and still re-runnable (`decisions/log.md` [2026-08-18]). **2026-08-18 (spec rev 37):** the **Step 8 review card** was re-laid-out onto the **same skeleton as the Step 8b execution panel** (rev 26) — header counts beside the title, one description, a bounded summary, *Show all*, then the footer — so the two consecutive Step 8 screens share one information architecture. The register-download note was pinned opposite the headline count in a `space-between` (wrapped into it at any narrow width; now under *Show all*); the four counts were prose not figures (now number-first `Stat`s on one inset panel); both states' hand-rolled footers became one shared `ReviewFooter`; the conflict state's impact tally moved **above** its cards. `ConflictCard` and the full change table are unchanged (`decisions/log.md` [2026-08-18]). **2026-08-18 (spec rev 38):** the **Live activity** feed's outcome badge is sized to the **widest outcome** (`Deferred`) instead of shrink-wrapping, so every row's description starts at the same x — the description carries the full path and wraps over two or three lines, so a varying badge width made every row wrap at a different column (routine now that a Step 11 guard refusal is `Deferred`, rev 30). Derived from the outcome enum, applied as a *minimum* width (an under-estimate widens rather than clips), with the reason line's indent from the same constant. The full audit trail already sized its Outcome column against the enum universe and is unchanged (`decisions/log.md` [2026-08-18]). **2026-08-18 (spec rev 39):** the **Document Register** became one list of everything a run produces — the **deleted folder manifest** moved out of its own card into the register list (it differs mechanically: a synchronous CSV from `…/folders/deleted/export`, so no row count and never *Generating*, which a shared row model absorbs); every item gained a **one-line description of what is in the file**; and titles now **name the artefact rather than the moment** — *Before (pre-run)* → **Starting register**, *After review (pre-normalization)* → **Reviewed plan**, *Name changes — before/after* → **Planned / Applied name changes**, *After (outcome)* → **Final outcome**. Order follows the run timeline with the manifest between the final outcome and the on-demand export; the purge confirmation now names the Document Register. `DeletedFolderManifestPanel` deleted (`decisions/log.md` [2026-08-18]). **2026-08-18 (spec rev 40):** two new records joined it — **Archived documents** (`GET …/documents/archived/export`) and **Purged documents** (`GET …/documents/purged/export`), both derived on demand from the **audit trail** via new single-definition types (`ArchivedDocuments`, `ArchivePurge`) mirroring `DeletedSourceFolders`; no new snapshot kind, no schema change, no background job. The purged record is explicit that its grain is an **inference**: `DeleteDomain` bins the archive as *one* entry, so MAGIQ confirms the destruction once at library level and never per document — the file prints the confirmations each row rests on. It lists nothing for a **rolled-back** run (a rollback returns every document and only then destroys the empty archive, leaving an identical trail) or when the only purge predates this attempt's last archival activity (the re-run guard, since `ClearRerunStateAsync` keeps the trail across attempts). **Known residual:** that guard is a heuristic, not attempt scoping — the archived export can still list a prior attempt's moves; proper scoping needs an attempt watermark on the run. **The C# is inspection-verified only — no .NET SDK in the authoring environment, so it still needs a `dotnet build` + `dotnet test`** (`decisions/log.md` [2026-08-18]). **2026-08-18 (spec rev 41):** the SPA gained **addressable pages** — `/runs`, `/runs/{runId}`, `/queries`, `/settings`, `/` → runs. The shell held its page in `useState`, so the app lived at one URL and a run could not be linked to. A hand-rolled `useRoute` hook over the History API (`src/routing/routes.ts`), **not** a routing library — four routes and one param don't justify one in an SPA with no routing/state library. **Nothing was needed server-side**: `MapFallbackToFile("index.html")` was already registered last, so deep links already resolved on IIS, Docker and Vite dev. Nav items and dashboard run rows became real anchors (modifier/middle-click reach the browser; the rows gained a keyboard tab stop they never had). The page segment matches **case-insensitively** — the fallback serves `/Runs/{id}`, and an exact match would drop the id and silently redirect to the list. **Limitation:** paths are root-absolute, so no IIS virtual-directory hosting (already true — `vite base` is `/`). Also fixed en route: `JobsDashboard`'s `TOTAL_STEPS` was still `11` (`decisions/log.md` [2026-08-18]). **2026-08-18 (spec rev 42):** the shared folder browser's order became **breadcrumb → filter box → folder list**, **reversing rev 34** — top-down that reads *where you are* → *how to narrow it* → *what is here*, the order a file manager leads with. Rev 34's case was adjacency (trail beside its list), but the filter box is itself about the current location, so it sits fine between the two. The up-folder control stays at the head of the breadcrumb row. One file, both callers: `components/FolderBrowser.tsx` holds the SPA's only breadcrumb (`decisions/log.md` [2026-08-18]). **2026-08-18 (spec rev 43):** **Step 12 destroys the archive in chunks** — `DeleteDomain` bins the library as *one* entry, so its purge was a single multi-minute call stuck at `0/1` and the tool looked hung during the one irreversible part of a run. Three named passes now: `DeleteFolder` per chunk (**one bin entry per folder** — the whole mechanism), purge those entries, then the existing `DeleteDomain` + `DeletePath` sweep over the emptied library and the Step 11 source folders. Chunks come from `ArchiveChunkPlanner` by **depth** — shallowest level beneath the archive destination with ≥ `ArchivePurgeChunkTarget` folders (default 25, preferring a shallower level over one exceeding 1000 pieces, `0` disables) — since a `DeleteFolder` already takes a whole subtree server-side, so only granularity is in question and one chunk per recorded folder is ~2× folder-count round-trips for the same information; single-depth chunks never nest, so deletes are order-independent. Planned from the run's own `CleanupRunArchiveFolder` rows because **chunking is an optimisation, never the guarantee** — every miss is still destroyed by the sweep behind it. New hazard, guarded: a chunk purge that went **indeterminate** is barred from the final sweep (keyed on delete path + name, not handle) rather than re-issued. **Sequential by default** (`ArchivePurgeConcurrency`, 1–8). Side effect: the purged-document record confirms per archive folder rather than once per library (still not per document). Chunk deletes carry a **null phase**, keeping them out of `DeletedSourceFolders`/the manifest. Fixed en route: the **rollback** teardown compared `Archive/2019/Case` with `\Archive\2019/Case` and so matched nothing, leaving its archive folders in the recycle bin (`decisions/log.md` [2026-08-18]). **2026-08-19 (spec rev 44):** four defects surfaced by a failing suite, three of them behaviours the spec already claimed. **(1)** `CandidateFolderBuilder.ResolveSelectedIds` skipped the acronym cascade's DFS at its **root** when that root was protected — and Rule 2 protection propagates *upward*, so one post-cutoff document anywhere beneath a matched case folder un-selected the whole folder: the 92%-left-behind defect, reintroduced. Root clause dropped; the protected folder is still withheld by the per-node guard and by `Build`'s `!isProtected`. `dev-spec.md` §pre-lock cascade contradicted itself here and is corrected. **(2)** **No CSV ever carried a UTF-8 BOM** — `UTF8Encoding(true).GetBytes()` doesn't emit the preamble, so all three writers (`RunOperationsCsvWriter`, `RegisterCsvWriter`, `CsvText.ToBytes`) shipped without one while five `dev-spec.md` endpoints promised it; every test stripped a BOM that was never there. Now prepended + byte-asserted in each. **(3)** rev 30's `Deferred` classification of a pre-delete guard refusal was applied to `DeleteEmptyFoldersAsync` only — `RetryFolderDeletesInlineAsync`, which the prune cycle calls automatically, kept writing red `Failed` rows. Now identical in both (the relax-exemption was already shared via `RunFolderGroupAsync`). **(4)** the prune's auto-skip is scoped to plans that **proved** `!ResolvesFolder`; a plan that claimed resolution then failed leaves the folder `Failed`, keeping Retry / What's in it? / prune available — spec corrected to match (Chase's call). The other three failures were stale tests (pre-rev-30, pre-[2026-08-14] always-restore) and one malformed assertion pair. **Inspection-verified only — needs `dotnet build` + `dotnet test`** (`decisions/log.md` [2026-08-19]).

## ADO Board
Project: **Documents** (MAGIQSoftware org). Epic **#34120** — *Document Lifecycle Cleaner — NATA* (8 Features, 17 User Stories, 49 Tasks; created 2026-07-13). Repo: `DocumentLifecycleCleaner`. Work items tagged `document-lifecycle-cleaner` and per-branch tags.

## Stack

- **Frontend:** React (review + confirmation UI — Steps 6 & 7)
- **Backend:** .NET API
- **Data:** SQL queries, system-level configurable
- **Integration:** MAGIQ Documents library/folder/document APIs

## Key Constraints

- SQL queries must be configurable at the system level (DB-backed `ConfiguredQuery` store, no hardcoded schema)
- Deletable folder acronyms **pre-select** folders for deletion in the UI; the operator may deliberately override an individual pre-selection. Folder **protection (a post-cutoff document) always wins** — a protected folder is never deletable
- Deletion is blocked until folder validation passes (delete constraint rule)
- Purge is a background system process, not a direct user action
- A folder rule that disallows deletes (a `Move` is a copy + delete) is handled reactively — read the live rule, temporarily relax it on the one folder, retry, then restore it (`decisions/log.md` [2026-08-05]). **Subtree form** for the Step 11 prune (`decisions/log.md` [2026-08-14]): when relaxing the immediate parent can't reach the blocking rule, relax `FolderDeletes` from the top of the blocked folder's subtree with `ApplyToTree`, retry deepest-first, restore the same way — gated on the rejection coming from MAGIQ (not the tool's own pre-delete check) *and* on the whole subtree being deleted, since a tree-wide set overwrites every descendant's rules
- **Completeness invariant (spec Rule 9, `decisions/log.md` [2026-08-14]):** never delete a folder whose contents a run hasn't fully evaluated. Step 4's `CandidateFolders` closure is **upward-only**, so a subfolder holding only post-cutoff documents can be invisible end-to-end. Enforced at execution by the Step 11 live pre-delete guard (`DeleteFolderIfEmptyAsync`, fail-closed); a blocked delete is then explained by the failures panel's **What's in it?** action (`GET …/folders/completeness`) — derived free from `CleanupRunFolder.FolderCount` vs. the child rows held, propagated up the ancestor chain, with only the directly-blocked folders live-verified. **Deliberately not surfaced at the Step 6/7 review** (built, then withdrawn 2026-08-14): there the selection is still just the acronym pre-selection that review is about to change, *and* the live half can't address a not-yet-normalized path, so it reported "Target folder not found" for exactly the folders worth checking. The derived counts are an inference, not a proof (Step 4 snapshot); the guard is the guarantee. A blocked delete is resolvable via the terminal `FolderStatus.Skipped` (`POST …/folders/skip`, mandatory reason, `FolderSkipped` audit row) — deliberately **not** `Protected`, which means known post-cutoff content. Finishing the work means a **new scoped run**, not adoption into the old one (its archive library may already be purged). The documents half of the invariant is the `409 UnresolvedMoveFailures` gate on `…/archival/continue`. **Empty-subtree prune (Step 11, second pass, `decisions/log.md` [2026-08-14]):** the downward closure, finally built — but driven from the *blocked* folders at execution time, not widened across the candidate set (which would pull in effectively the whole library). New configurable `FolderSubtree` query reads a blocked folder's full descendant closure live from the MAGIQ DB with per-folder document counts; `SubtreePrunePlanner` marks a descendant prunable only when its **entire subtree holds zero documents** *and* the run never evaluated it (an evaluated one is the normal pass's business, or a deliberate keep). Operator-gated: `GET …/folders/prune-plan` (dry run) → `POST …/folders/prune` (re-derives the plan live before executing, so a document added during review can't be destroyed), each delete still through the live guard, audited as `DeleteFolder` + `Detail = "Empty-subtree prune"`. Rule 9 is about *evaluation*, so extending the evaluation satisfies it
- **Name normalization (Step 8, implemented):** the SOAP `Move`/`CreateFolder` can't handle names carrying whitespace variants (double space, non-breaking `U+00A0`, and other Unicode spaces) or invisible format chars (zero-width, BOM, soft hyphen) — all of which the desktop UI allows and which are easily pasted from other documents. A pre-archival phase renames the offending **source** docs/folders in place (any Unicode whitespace → a single regular space, collapsed/trimmed; invisibles stripped — full lists in spec §"Whitespace normalization scope") via `UpdateFolderProperties`/`UpdateDocumentProperties`. The phase runs **8a dry run → conflict gate → 8b execute**: because normalizing can collapse two names onto one, a non-mutating dry run detects name conflicts, the run pauses (`AwaitingInput`) in the **Normalization Review** view for the operator to resolve each (rename / merge folders / keep-one-delete-other; safe rename pre-selected, destructive options confirmed + audited), re-analyses until clean, then executes. Every rename/merge/delete is recorded, and **once 8b executes its first change the run becomes an audit — no longer deletable, only archived** (spec Rules 7 & 8; `decisions/log.md` [2026-08-05, 2026-08-07])

## Blocking Open Questions (all resolved — historical)

Both were resolved long ago; architecture and implementation are complete. Retained for the record:

1. ~~**Who sets the specified date and how?**~~ — ✅ Resolved: the operator picks the cutoff date **and** source library at run creation (per-run inputs on the `CleanupRun`).
2. ~~**Does folder protection extend to the full ancestor hierarchy or immediate parent only?**~~ — ✅ Resolved: full ancestor hierarchy protected (maximum protection).

Full resolution history is in `notes.md` and `decisions/log.md`.

## Scope

Delivered: spec finalised → React UI + .NET API implemented → Epic 34120 shipped (as-built Steps 1–12, including the **Name Normalization** phase at Step 8). Current focus: the deferred/post-MVP backlog (`deferred-work-plan.md`) and audit-trail/register polish.

## File Map

| File | Purpose |
|------|---------|
| `brief.md` | Project summary and constraints |
| `notes.md` | Open question resolutions and session notes |
| `risks.md` | Risk register |
| `tasks.md` | Task tracking |
| `dev-context.md` | Cross-project working context: 3-repo topology, settled architecture, the FastEndpoints→command/query→context pattern, branch/PR protocol |
| `delivery-plan.md` | Epic 34120 ordered branch plan — 17 stories → branches, per-branch scope, dependencies |
| `normalization-conflict-gate-plan.md` | Implementation plan for the Step 8 conflict dry-run + resolution gate (API + SPA): current→target, data model, ordered branches B1–B8, test matrix, DoD |
| `decisions/log.md` | Architecture and design decisions (append-only log) |
| `spec/NATA_Document_Lifecycle_Cleaner_Spec_v0.6.md` | Spec — source of truth (active, markdown) |
| `spec/dev-spec.md` | Developer specification — API catalogue, data model, config schema, SignalR contract, component map, sequence flows, error contracts |
| `references/adrs/ADR-001-hangfire-background-engine.md` | ADR: Hangfire as pipeline engine |
| `references/adrs/ADR-002-cleanup-run-state-machine.md` | ADR: CleanupRun persisted state machine |
| `references/adrs/ADR-003-multi-target-hosting.md` | ADR: IIS default + Docker supported |
| `references/adrs/ADR-004-magiq-integration-soap-dapper.md` | ADR: MAGIQ integration via SOAP + Dapper |
| `references/adrs/ADR-005-single-application-database.md` | ADR: Single dedicated app database |
| `references/adrs/ADR-006-authentication-two-ticket-model.md` | ADR: MAGIQ auth piggybacking + two-ticket model |
| `references/adrs/ADR-007-process-ticket-persistence.md` | ADR: Process ticket persisted in app DB |
| `references/adrs/ADR-008-react-spa-from-wwwroot.md` | ADR: React SPA served from API wwwroot |
| `references/adrs/ADR-009-command-query-dispatch.md` | ADR: Command/query dispatch via FastEndpoints built-in bus |
| `spec/NATA_Document_Lifecycle_Cleaner_Spec_v0.3.md` | Spec — previous version (archived, markdown) |
| `spec/NATA_Document_Lifecycle_Cleaner_Spec_v0.2.docx` | Spec — previous version (archived) |

## Decisions

All architecture and design decisions go in `decisions/log.md`. Do not duplicate entries in `brief.md` or here.

---

## Spec Alignment — keep the spec in step with the repo

**The spec is the source of truth for *intent*; the repo is the source of truth for *behaviour*. They must not drift.** Whenever work in the `DocumentLifecycleCleaner` product repo changes behaviour that the spec describes — or adds behaviour the spec should describe — **the spec is updated in the same unit of work**, before the change is considered done. Treat a spec update as part of the definition of done, not a follow-up.

**What "the spec" means here:** the business/behaviour spec `spec/NATA_Document_Lifecycle_Cleaner_Spec_v0.6.md` (source of truth) **and** its developer companion `spec/dev-spec.md` (API catalogue, data model, config schema, SignalR contract, sequence flows, step numbers). A change usually touches one or both; check both. Architecture/design *rationale* still goes to `decisions/log.md` + `references/adrs/` — the spec records *what the system does*, the log records *why*.

**When a repo change lands (or is being designed), check it against the spec and reconcile:**

- **New or changed behaviour** — endpoint, pipeline phase/step, run state, rule, SOAP op, query contract, config/setting, UI flow, audit/log surface: update the matching spec section (and dev-spec section) so it describes the built behaviour. Bump the `_Last updated:_` date and add a one-line note under **History**.
- **Step/phase renumbering or renaming** — update every affected reference in both specs (status banner, phase table, step headings, flow diagrams, endpoint table, `RunPhase`/state tables). Grep for the old numbers so none are left stale.
- **Something the spec still calls "specified / not-yet-built" that has now shipped** — flip it to as-built and remove the not-implemented caveats.
- **A repo change that contradicts a spec rule or a decision** — do **not** silently rewrite the spec to match. Flag the conflict (surface it to Chase, and note it against `decisions/log.md`) and reconcile the intent first; only then update the spec.

**Keep this CLAUDE.md and `MEMORY.md` honest too** — if the status/step summaries above no longer match the repo, correct them as part of the same reconciliation.

If in doubt, a quick pass is: *did this change what the system does? → find the spec section that claims otherwise → fix it (or flag it) now.*

---

## Memory System

**MEMORY SYSTEM**

This folder contains a file called `MEMORY.md`. It is your external memory for this project — use it to bridge the gap between sessions.

**At the start of every session:** Read `MEMORY.md` before responding. Use what you find to inform your work — don't announce it, just be informed by it.

**Memory is user-triggered only.** Do not automatically write to `MEMORY.md`. Only add entries when the user explicitly asks — using phrases like "remember this," "don't forget," "make a note," "log this," "save this," or "create session notes." When triggered, write the information to `MEMORY.md` immediately and confirm you've done it.

**All memories are persistent.** Entries stay in `MEMORY.md` until the user explicitly asks to remove or change them. Do not auto-delete or expire entries.

**Flag contradictions.** If the user asks you to remember something that conflicts with an existing memory, don't silently overwrite it. Flag the conflict and ask how to reconcile it.

---

> When the user asks to create a new subfolder, use the **subfolders** skill. It handles the full interview, CLAUDE.md and MEMORY.md creation, identity overrides, and memory isolation.
